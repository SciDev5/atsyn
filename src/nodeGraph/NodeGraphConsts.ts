export default class NodeGraphConsts {
    static readonly gridSize = 50;

    static readonly svgBorder = {
        margin: 60,
        step: 100,
    } as const;

    static readonly scrollSpeed = 0.003;

    static readonly rowPaddingBlock = 2;
    static readonly rowBorderTop = 1;
}